pipeline {
    agent any
    stages {
        stage('Check PATH') {
            steps {
                sh 'echo $PATH'
                sh 'which npm'
            }
        }
        // ... rest of stages
    }
}
